@include("asm.silver");

func printf(str: &u8);
func putchar(char: u8);

type strcontainer = {data: &u8, len: u64};

<T> func test(var: T) = {
  printf(var.data);
};

func main() : u64 = {
  string : strcontainer;
  &(string.data) = "hella";
  test<strcontainer>(string);
  0
};
