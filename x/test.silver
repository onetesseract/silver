#prec 15;
infix ==(a: u64, b: u64) : bool = @AS("int_cmp_eq a b");

func printf(str: &u8);

enum a = {
   b,
   c
};

func test_enum(enum: a) : a = {
   if enum == a#c {
      printf("C!");
   };
   a#b
};


func main() : u64 = {

  test_enum(test_enum(a#c));

  0
};
