putchar(c u64);

type test = &u64;

#prec 10;
(a test) = (b u64) = #AS("store_in a b");

print(char test) = putchar(* *char);

<T> type x = T;

<T> im_losing_my_mind(somevar x<T>) = print(somevar);

main() : u64 = {
  K x<u64>;
  K = 80;
  im_losing_my_mind<u64>(*K);
  a u64;
  a = 73;
  print(a);
  putchar(*a);
  0
};
