@include ("asm.silver");
/* @include ("array.silver"); */

printf(str &u8);
putchar(char u8);

type strcontainer = {data &u8, len u64};

<T> test(var T) = {
  printf(var.data);
};

main() : u64 = {
  string strcontainer;
  &(string.data) = "ello";
  printf(string.data);
  0
};
