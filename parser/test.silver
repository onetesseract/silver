main() : u8 = {
  a u8;
  a = (b as u8);
  0
};

main();                              // main()

pop(list &vec) : u8;                 // pop(list) OR pop list

new (list &vec) : &vec               // new(list) OR new list

(list &vec).pop() : u8;              // list.pop()

(list &vec) as_string : &char;       // list as_string

(source &ptr) into (dest &ptr) : u8; // list.pop() into somevar

#inline
#linkage internal

#inline, linkage internal;
(u8 a) + (u8 b) : u8 {
  return #AS("add u8 $a $b")
}
