printf(val &u8) void;

string {
  buf &u8;
  self &string =(other &string) void = printf("not impl kekw");
};

main(argc u64, argv &&u8) u64 = {
  str string;
  str.buf = "ee";
  printf(str.buf);
  0
};
