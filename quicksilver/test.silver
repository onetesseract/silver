printf(val &u8) void;

print_argc_recursive(count u64, index u64, argv &&u8) void = {
  printf(argv[index]);
  if (index == count) printf("end of args") else {printf("\n"); print_argc_recursive(count, index + 1, argv)}
};

main(argc u64, argv &&u8) u64 = {
  print_argc_recursive(argc, 0, argv);
  0
};
